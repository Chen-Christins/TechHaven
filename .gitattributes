# 统一行尾为 LF，与 .prettierrc.cjs 的 endOfLine: "lf" 保持一致。
# 此前仓库无 .gitattributes，历史 blob 混有 CRLF/LF，导致每次 format 后全树漂移。
* text=auto eol=lf

# 二进制资源不做行尾转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
*.woff binary
*.woff2 binary
*.ttf binary
*.eot binary
*.zip binary
*.mp3 binary
*.mp4 binary
